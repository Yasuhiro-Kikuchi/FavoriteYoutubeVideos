<h2>※「<%= @genre.name %>」の投稿</h2>
    <% @data.each do |obj| %>
    <div class="articles">
        <class="article">
            <h3 class="top_title"><a href="/videos/show/<%= obj.id %>"><%= obj.title %></a></h3>
            <iframe width="350" height="200" src="<%= obj.url %>" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
            <p class="top_read"><%= obj.read %><span class="top_created">(<%= obj.created_at %>)</span></p>
    </div>
    <% end %>
<!--<% @data.each do |obj| %>-->
<!--    <h3 class ="top_title"><a href="/videos/show/<%= obj.id %>"><%= obj.title %></a></h3>-->
<!--    <p class="top_read"><%= obj.read %><span class="created">(<%= obj.created_at %>)</span></p>-->
<!--    <hr>-->
<!--<% end %>-->
<div class="navigate">
    <% if @page_num > 0 %>
        <a href="/videos/genre/<%= @genre.id.to_s + "/" + (@page_num - 1).to_s %>">＜＜前のページ</a>
    <% end %>
    <span> </span>
    <a href="/videos/genre/<%= @genre.id.to_s + "/" + (@page_num + 1).to_s %>">次のページ＞＞</a>
</div>